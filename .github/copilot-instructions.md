# Copilot Instructions

## Project Guidelines
- In the HotA map editor plugin project, the garrison feature raises the limit on the number of garrison *objects* on the map from 48 to 255 — not creature stack size. The engine natively supports 255 garrisons, but the editor was artificially capped.